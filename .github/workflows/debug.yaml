name: Debug Test

on: [workflow_dispatch]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print working directory
        run: |
          pwd
          ls -la
          ls -la Project-Udacity-4/movie-app/frontend
